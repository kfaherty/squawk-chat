<div class="app-contain">
    <div class="top-bar">
        <div class="logo-contain">
            SquawkChat
        </div>
        <nav class="text-buttons-contain">
            <span class="active">{{#link-to 'messages' class="text-button"}}Messages{{/link-to}}</span>
            <span>{{#link-to 'channels' class="text-button"}}Channels{{/link-to}}</span>
            <span>{{#link-to 'search' class="text-button"}}Search{{/link-to}}</span>
            <span>{{#link-to 'search' class="text-button"}}Settings{{/link-to}}</span>            
        </nav>
    </div>

    {{user-list}}

    {{outlet}}
</div>